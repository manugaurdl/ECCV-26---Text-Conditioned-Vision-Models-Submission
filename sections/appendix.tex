\newpage
\appendix
\section{Appendix}

\subsection{Parallels to Human Image Perception}

% Instead, inspired by neuroscience evidence that suggests that humans fundamentally parse images differently based on the textual prompt.
% % For those
% % Motivated by human vision, there exists evidence to suggest that people parse images quite differently depending on the textual prompt, often manifested as top-down task-guided attention~\cite{buswell1935people} (example in supplement). 
% In the paper - "In human vision, studies indicate that people parse images differently depending on prior priming using a text prompt~\cite{buswell1935people} (example in supplement)."
\cref{fig:gaze_cogsci} illustrates how human gaze patterns over the same image shift depending on a task-relevant textual prompt~\cite{buswell1935people}, providing inspiration from neuroscience for \model{}'s early fusion of language into the visual encoding process.


\begin{figure}[h!]
\centering
\includegraphics[width=0.8\textwidth]{figures/gaze_cogsci.png}
\label{fig:gaze_cogsci}
\end{figure}

% \subsection{Caption Length and Localization}

% \begin{figure}[h!]
% \centering
% \includegraphics[width=0.5\textwidth]{old_figures/token_len.png}
% \caption{\textbf{\JR{remove?!} Denser captions improve localization.} On the Ref4 benchmark, referential segmentation performance increases with caption length, confirming that richer text conditioning yields more precise spatial steering.}
% \label{fig:ref4_token_len}
% \end{figure}

\subsection{Training Data}
\label{apdx:data}

We train on a mixture of referential segmentation and grounding datasets to ensure diversity in both visual domains and textual expression styles:

\noindent \textbf{RefCOCO, RefCOCO+, RefCOCOg}~\cite{kazemzadeh2014referitgame, yu2016refcocog} provide referring expressions grounded in COCO images.
RefCOCO+ excludes spatial language (e.g., ``left of''), encouraging the model to rely on appearance cues, while RefCOCOg contains longer, more descriptive expressions that exercise the model's capacity for detailed textual understanding.

\noindent \textbf{Visual Genome}~\cite{krishna2017visual} (preprocessed following MDeTr~\cite{kamath2021mdetr}) contributes region descriptions paired with bounding boxes across densely annotated scenes.
These descriptions span a broader vocabulary and more complex spatial relationships than the RefCOCO family, increasing the diversity of text conditioning signals.
We use SAM2~\cite{ravi2024sam2} to convert bounding boxes to binary segmentation masks.

\noindent \textbf{Mapillary Vistas}~\cite{neuhold2017mapillary} provides street-level imagery with fine-grained panoptic annotations.
Including this dataset expands the visual domain beyond COCO
% \MT{\st{COCO's indoor and object-centric bias toward outdoor, driving, and urban scenes,}} 
improving out-of-distribution generalization.
We adopt Describe Anything's~\cite{lian2025describeanything} synthetic
referential expressions along with accompanying segmentation masks.

Overall, our combined dataset of 133k unique images and 1.91M image-text pairs exposes the model to varied scene complexities (from single objects to dense urban panoramas), expression lengths (from two-word labels to multi-sentence descriptions), and visual domains (indoor, outdoor, street-level), encouraging robust steered representations. \MG{Would be nice to have a sunburst chart}

\subsection{Baseline Feature Extraction}
\label{app:baseline_extraction}

This section provides more details of how we extract text-conditioned (or text-fused) visual features from each baseline family evaluated in \cref{sec:experiments}.
For all baselines, global feature aggregation follows the convention of the base method (e.g., \texttt{[CLS]} token or mean pooling) unless stated otherwise.

\paragraph{Cross-Modal Encoders (CLIP \cite{radford2021_clip}, SigLIP \cite{zhai2023_siglip}).}
Images and language inputs are embedded independently by their respective encoders.
We combine the resulting visual and text feature vectors via element-wise addition (late fusion), the simplest form of post-hoc multimodal interaction.

\paragraph{MLLMs (InternVL3~\cite{zhu2025internvl3}, Qwen3-VL~\cite{Qwen3-VL}).}
\sout{We concatenate the user prompt~$X_t$ followed by the image~$X_v$ into a single multimodal input sequence and extract the last-language-block hidden states at image-token positions as dense, text-contextualized visual features.
For global features, we adapt the last-token pooling approach of E5-V~\cite{Jiang2024E5VUE}: a postfix is appended instructing the model to summarize the image contents in one word while focusing on the aspect specified by~$X_t$.}
\MG{We feed the text prompt~$X_t$ and image~$X_v$ as a single multimodal input sequence and extract hidden states from the last LLM layer.
For dense features, we take the hidden state at each image-token position.
For global features, following E5-V~\cite{Jiang2024E5VUE}, we append an instruction asking the model to summarize the image in one word in addition to the initial text prompt $X_t$
% while focusing on the visual concept specified in ~$X_t$ 
and extract the last-token hidden state.}

\paragraph{Open-Vocabulary Localization (SAM3~\cite{carion2025_sam3}, GroundingDINO\cite{liu2023groundingdino}).}
\MG{We ignore decoder outputs and extract intermediate features from the multimodal encoder.}
For SAM3, we take the patch-level outputs of the \textit{Fusion Encoder} and apply mean pooling to obtain global image features.
For GroundingDINO, we use the outputs of the cross-modal \textit{Feature Enhancer} layer.
Because features are available at multiple spatial scales, we first interpolate them to a common intermediate resolution before averaging to obtain dense pseudo-patch-level features. \MG{These are mean pooled to yield global features.}

\subsection{Assessing Representational Steerability with CORE}
\label{apdx:CORE}

For our \textit{COnditional REtrieval} (CORE) benchmark introduced in \cref{subsec:cond_ret_exp}, we select select 100 base images for each of six scenes (three indoor and three outdoor environments) from SUN397~\cite{Herranz2016_SUNdataset}. For each image, we create five separate instances by inpainting a \MG{scene-relevant background object (for e.g., an ``olive oil bottle'' in ``kitchen'')} using FLUX.2 [dev] \cite{flux-2-2025}, resulting in a total of 500 images per scene. 
The selected scenes, the corresponding give objects added to each scene and the text-conditioning prompts are reported in Table~\ref{tab:core_object_prompts}.

\MG{With each image as a query and rest 499 as the retrieval database, we frame the problem as one-vs-all image retrieval. This evaluates the visual encoder's ability to steer its global representation using the text prompt in order to retrieve the images comprising the objects specified in the prompt}
% To test the ability of visual encoders to steer their global representations in order to retrieve images relevant to a specified prompts, we frame the problem as one-vs-all retrieval. 
For each image $X_v^\Omega$ containing object $\Omega$, the objective is to retrieve other instances including $\Omega$. Here, all images (query and gallery instances) are encoded with the same object-specific prompt $X_t^\Omega$ (cf.~\cref{tab:core_object_prompts}). For a given query image, the gallery is comprised of 495 variations with non-identical base images of the same scene. The steerability score is calculated using top-1 retrieval accuracy and averaged across query images and object types.

\input{tables/core_prompts}
\MG{TO DO}
We report detailed quantitative results on a per-scene basis in Table \ref{tab:knn_exp_quant_full}. \model{} consistently boasts the best performance across all environments, demonstrating how its global image representations get shaped and steered by the language input. When prompted with an incorrect description of a randomly chosen different object (\xmark), performance drops drastically, indicating the tight integration and important role of the textual inputs. Cross-modal vision encoders (blue), on the other hand, see negligible changes in this setting. 

\input{tables/knn_exp_apdx}

Figure \ref{apdx_fig:core_qual} illustrates qualitatively how retrievals differ for a prompt-agnostic generic vision model like DINOv2 (blue) and our prompt-aware \model{} (green) for one indoor and outdoor scene each. 
Whereas DINOv2 mostly encodes scene-level appearance and retrieves visually similar images, \model{} focuses on the objects specified in the prompt and retrieves images containing those.
Despite many of the inpainted object being very small and in the background, the global features incorporate them sufficiently to allow for highly accurate retrievals. 

\begin{figure}[ht]
\centering
\includegraphics[width=1.0\textwidth]{figures/core_qualitative_retrieval_comparison_kitchen_anot.pdf}
\includegraphics[width=1.0\textwidth]{figures/core_qualitative_retrieval_comparison_park_anot.pdf}
\vspace{-3mm}
\caption{\textbf{Qualitative image retrieval results on CORE benchmark.} \colorbox{visionblue}{DINOv2} retrieves images mostly based on global scene similarity with little regard for the object of interest. In contrast, \colorbox{\methodcolor}{\model{}} effectively considers the task prompt and ranks images with the correct object highest, despite their innocuous background placement and comparably small size.
% Despite the innocuous background placement of objects and their comparably small size, our text-conditioned model robustly identifies other occurrences.
}
\vspace{-4mm}
\label{apdx_fig:core_qual}
\end{figure}

\subsection{MOSAIC Benchmark}

% \JR{Further describe how MOSAICs are acquired (e.g., resizing, background color)}

% The MOSAIC Localization benchmark introduced in \cref{subsec:mosaic_exp} is constructed to investigate how models aggregate global representations internally and how this process is affected by text-conditioning. 
% To ensure the absence of singular salient objects, four images from PASCAL-VOC~\cite{pascal_voc} are padded to an $1:1$ aspect ratio and stitched together into a single $2\times2$ mosaic. 
% For each mosaic, pixel-wise segmentation masks are constructed using the original dataset ground-truth and instances of the same class are considered across all four images.
\MG{The MOSAIC benchmark introduced in \cref{subsec:mosaic_exp} evaluates whether text conditioning can steer the self-attention of the \texttt{[CLS]} token toward patch tokens corresponding to a prompted object.
We stitch four PASCAL-VOC~\cite{pascal_voc} images (padded to $1{:}1$ aspect ratio) into a single $2\times2$ mosaic to eliminate a single dominant salient object.
For a given text prompt specifying an object class, the ground-truth is a binary segmentation mask over the mosaic's patch grid, constructed from instances of that class across all four sub-images.}

Figure~\ref{apdx_fig:steer_attn} provides more qualitative examples of attention maps on such multi-image mosaic. 
% Following the same methodology as specified in \cref{subsec:mosaic_exp} and used in \cref{fig:steer_attn}, we provide additional qualitative examples of attention maps on multi-image mosaics in \cref{fig:steer_attn}. 
Again, we see a strong propensity of DINOv2 to focus on the most dominant object(s) within the collage. \model{} selectively attends to the objects or entities specified in the prompt (\cref{apdx_fig:steer_attn}, top). Despite being trained with single-instance data (i.e., only one object corresponding to prompt), 
% a more complex behavior emerges that allows the model to attend to multiple concept instances, even across sub-images in the mosaic (\cref{apdx_fig:steer_attn}, middle). 
\MG{we find that our model can localize multiple instances of an object class across images in mosaic (\cref{apdx_fig:steer_attn}, \model{} attends to all instances of ``chair'' in middle row).
We discuss this emergent multi-localization property of our method in ~\cref{app:training_signal}}
\model{} also considers described attributes and routes global attention accordingly (e.g., ``black sheep'' vs. ``white sheep'' in \cref{apdx_fig:steer_attn} (bottom)).

\begin{figure}[ht]
\centering
\includegraphics[width=0.8\textwidth]{figures/mosaic_attention_maps_219.png}
\includegraphics[width=0.8\textwidth]{figures/mosaic_attention_maps_338.png}
\includegraphics[width=0.8\textwidth]{figures/mosaic_attention_maps_45.png}
\vspace{-3mm}
\caption{\textbf{Effective prompt-based attention steering.} \colorbox{visionblue}{DINOv2} focuses on dominant image regions. \textbf{Top:} \colorbox{\methodcolor}{\model{}} can attend to specific objects-of-interest. \textbf{Middle:} Despite single-instance training, it can consider multiple occurrences of the specified concept. \textbf{Bottom:} specifying attributes (e.g., color) places focus on specific entities. 
}
\vspace{-4mm}
\label{apdx_fig:steer_attn}
\end{figure}

\subsection{Assessing Visual Representation Quality}

\JR{details on eval methodology}
\JR{what prompts where used for each dataset}


\subsection{Gate Steering}

\JR{Additional gate steering results (>1.0) + (<0.0 -> emerging ignoring behavior)?}
\JR{Yuki: qualitative results may suffice}

\subsection{Anomaly Segmentation}

\begin{figure}[ht]
\centering
\includegraphics[width=1.\textwidth]{figures/anomaly_detection_grid_carpet_leather.png}
\includegraphics[width=1.\textwidth]{figures/anomaly_detection_metal_nut_hazelnut_bottle.png}
\vspace{-3mm}
\caption{\textbf{Anomaly segmentation heatmaps predicted by \model{}.} We use the linear classification head optimized for patch-wise referential segmentation. Despite the stark task and domain gap, performance is especially good on texture-based classes (top) but also allows \model{} to highlight small defects in objects (bottom).
}
\vspace{-4mm}
\label{apdx_fig:anomaly_detection_examples}
\end{figure}

We perform zero-shot anomaly segmentation (AS) on the MVTec~AD~\cite{bergmann_mvtech} and VisA~\cite{zou2022_visa_dataset} datasets.
We derive the anomaly map by upsampling the continuous heatmap produced by the learned linear segmentation head.
As for all text-steerable methods, we use an ensemble of ten anomaly prompts (e.g., \texttt{``the anomaly in the <object>.''}) and average the resulting heatmaps across prompts.
Following standard practice in the AD literature~\cite{WinCLIP_Jeong23CVPR}, we measure the pixel-wise \textit{Area under the Receiver Operating Characteristics} ($\text{ROC}_P$), the \textit{Per-Region-Overlap} (PRO), and threshold-optimal $F_1$ score ($F_1^{\max}$).
Performance of existing training- and adaptation-free AS baselines is reported as stated in the original publication.

\input{tables/anomaly_detection_table}

\model{} again matches dedicated methods on this extreme OOD setting.
As shown in Table~\ref{tab:mvtec_visa}, \model{} reaches 79.8 PRO on MVTec~AD, substantially outperforming off-the-shelf segmentation-based approaches (SAM3 at 54.5, CLIPseg at 34.6) and closing much of the gap to specialist methods such as FADE (84.5).
On VisA, \model{} surpasses FADE on $\text{ROC}_P$ (90.2 vs. 91.5) and ties at 79.3 PRO, indicating robust transfer to a harder, more diverse inspection setting.
Qualitative examples of \model{}'s predictions on a subset of object types and defect categories are provided in Figure \ref{apdx_fig:anomaly_detection_examples}. Whereas the predicted heatmaps are especially accurate for texture-based inputs, the zero-shot setting makes it impossible to accurately predict certain defects (e.g., flipped metal nut), as no training on non-anomalous instances and no other visible defects (e.g., scratch) are present. 

\subsection{Additional Ablations on Training Design Choices}

% ------------------------------------------------------------------
\subsubsection{Training Duration}
\label{subsec:training_duration}

Steerability emerges rapidly: within 100k iterations, CORE accuracy reaches 85.5 (vs.\ 43.5 for frozen DINOv2), while FG-CLS degrades only modestly to 82.8.
However, tasks that require deeper language understanding continue improving with longer training: between 100k and 500k iterations, PODS improves from 35.0 to 55.3
%(+58\%)
and RefCOCOg from 58.8 to 71.5,
%(+25\%)
even as steerability saturates by 300k iterations at 92.5\%.
This pattern holds for both early and late fusion (late-fusion PODS: 39.1 at 300k vs.\ 45.3 at 500k), suggesting that longer training instills richer multimodal representations rather than merely teaching the model to route attention.
We adopt 500k iterations for all final models.

% ----------------------------------------------------------------
\subsubsection{Scaling}
\label{subsec:scaling}
\input{tables/scaling_table}

\paragraph{Visual Encoder.}
We scale the frozen DINOv2 backbone from ViT-S through ViT-B to ViT-L, and the SigLIP backbone from ViT-B to ViT-L.
% TODO: fill in numbers and interpretation once results are ready. Expected framing:
% Larger backbones improve both FG-CLS and steerability, with DINOv2 ViT-L achieving
% XX.X steerability and XX.X PODS.
% For SigLIP, scaling from ViT-B to ViT-L yields similar gains.
% This confirms that \model{}'s cross-attention layers benefit from richer
% frozen features without requiring retraining of the adapter.

\paragraph{Text Encoder.}
We compare RoBERTa-Base (125M parameters) against RoBERTa-Large (355M) as the frozen text encoder.
% TODO: fill in numbers and interpretation once results are ready. Expected framing:
% RoBERTa-Large improves PODS by X points, consistent with the finding
% that downstream text understanding benefits from richer language representations.
% Steerability is comparable, suggesting that the basic ability to route attention
% does not require a large language model.

% --------------------------------------------------------------------------------------------------------------------------------

\subsubsection{Role of FFN.}

% MAE results really good without FFN. FFN doesn't really add much in dino as well (2% in PODs)

% Each gated cross-attention block includes a similarly gated feed-forward network applied after cross-attention (\cref{eq:gated_ca}).
% Removing FFN yields comparable steerability (95.9)
% % and ADE20k (55.1)
% performance, with a modest drop on PODS (53.4 vs.\ 55.3).
% We retain FFN because it provides consistent gains on fine-grained discrimination; as shown in \cref{tab:cross_backbone}, this gap widens for backbone families with weaker self-supervised features (MAE scores 14.2)



\subsubsection{Layer Placement.}
We insert gated cross-attention at every third ViT block starting from block 2 (blocks 2, 5, 8, 11 for ViT-B/14; \cref{sec:arch}).
To understand which layers benefit most from text conditioning, we compare four alternative insertion patterns: early only (blocks 1, 3), early+mid (blocks 1, 3, 5, 7), mid+late (blocks 5, 7, 9), and late only (blocks 7, 9).
\MG{need to rerun without FFN; not that necessary but time permits}

% --------------------------------------------------------------------------------------------------------------------------------

\subsection{Training Signal: Pointing vs.\ Segmenting}
\label{app:training_signal}

As discussed in \cref{subsec:training_signal}, the choice of patch-level supervision signal---a Gaussian point heatmap versus a tokenized segmentation mask---encodes fundamentally different inductive biases that shape what the model learns about localization.

\paragraph{Inductive Bias Analysis.}
The Gaussian heatmap places a single peak at the bounding-box centre and applies a fixed-width blur ($\sigma{=}1.1$), producing a unimodal target that is invariant to object size and shape.
This trains the model to collapse attention to a single spatial location, rewarding centre prediction over region understanding.
The segmentation-mask target, by contrast, distributes probability uniformly across all tokens that overlap the referent.
This teaches token-level region matching: the model must determine \emph{which} tokens belong to the object rather than simply \emph{where} its centre lies.

\paragraph{Emergent Multi-Instance Localization.}
Because the mask signal rewards activating every overlapping token, the cross-attention layers learn a content-matching function: given a text query, each visual token independently receives high activation if its content matches the description.
At inference, this generalizes to multiple instances---when four apples appear in a scene and the query is ``apple,'' all four regions activate simultaneously, even though training annotations label only a single referent per sample.
The Gaussian-trained model lacks this capability: its unimodal supervision explicitly discourages distributed activations, so it consistently highlights only one instance.

% TODO: add qualitative figure showing multi-instance activation maps for mask vs. Gaussian models.
% \begin{figure}[t]
% \centering
% \includegraphics[width=\linewidth]{figures/multi_instance_qual.pdf}
% \caption{Multi-instance localization. Given the query ``apple,'' the mask-trained model (right) activates on all instances, while the Gaussian-trained model (left) collapses to a single peak.}
% \label{fig:multi_instance}
% \end{figure}